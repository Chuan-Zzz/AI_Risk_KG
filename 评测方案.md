# AI Risk Knowledge Graph 评测方案

> Event-centric, Evidence-constrained Extraction Pipeline — Evaluation Plan

---

## 1. 评测总览

### 1.1 评测目标

本评测围绕三个核心能力展开，对应方法的三大创新点：

| 评测维度 | 对应方法创新 | 核心问题 |
|----------|-------------|---------|
| **Extraction Accuracy** | 分层抽取 | 抽取对不对？ |
| **Event-level Aggregation Quality** | 事件级建模 | 多文档合并有没有用？ |
| **Graph Reliability** | 证据约束图谱 | 结果可信吗？ |

### 1.2 评测范围

| 范围 | 说明 |
|------|------|
| Pipeline 阶段 | Stage 2 ~ Stage 5（Stage 1 为输入整理，不参与评测） |
| 实体类型 | 41 种本体类（OntologyClass） |
| 关系类型 | 37 种关系（RelationType） |
| 抽取模式 | explicit / abstracted / inferred / completed |

### 1.3 评测论文表格规划

| 表格 | 内容 | 对应章节 |
|------|------|---------|
| Table 1 | Entity Extraction: Precision / Recall / F1 | §3.1 |
| Table 2 | Risk Chain Accuracy: Risk / Consequence / Impact | §3.2 |
| Table 3 | Event-level: Event Accuracy / Aggregation Gain | §4 |
| Table 4 | Graph Reliability: Evidence Coverage / Consistency Rate / Support Strength | §5 |

---

## 2. 数据集设计

### 2.1 数据集构成

| 数据集 | 规模 | 用途 |
|--------|------|------|
| **Full Evaluation Set** | 500 events | 全量评测，覆盖各类事件规模 |
| **Gold Standard Set** | 100 events（从 500 中精选） | 高质量人工标注，用于核心指标计算 |

### 2.2 Gold Standard 选取标准

从 `data/eval_dataset_500.json` 的 500 个事件中，按以下标准筛选 100 个：

1. **事件规模分布**：小事件（1-5篇）50个 + 中事件（6-30篇）30个 + 大事件（30+篇）20个
2. **文本质量**：字段完整度 ≥ 5.0，正文长度 ≥ 5000 字符
3. **领域多样性**：覆盖医疗、教育、金融、社交、安全等多个 AI 应用领域
4. **语言多样性**：英文事件约 70 个，中文事件约 30 个

### 2.3 标注方式

**流程**：LLM 预标注 → 人工校正 → 交叉审核

| 步骤 | 方法 | 说明 |
|------|------|------|
| Step 1 | Pipeline 自动抽取 | 对 100 个事件运行完整 Pipeline，生成初始标注 |
| Step 2 | 人工逐条校正 | 标注员对照原文，逐条检查抽取结果的正确性 |
| Step 3 | 交叉审核 | 第二位标注员独立审核 20% 样本，计算 IAA |

### 2.4 分层标注内容

#### Level 1：显式实体标注（Stage 2 输出）

| 字段 | 标注内容 | 示例 |
|------|---------|------|
| AISystem | AI 系统名称 | ChatGPT, K5 autonomous robot |
| AIModel | AI 模型名称 | GPT-4, DALL-E |
| AITechnique | AI 技术 | deep learning, NLP |
| AICapability | AI 能力 | text generation, image recognition |
| Stakeholder | 组织/个人 | OpenAI, School District |
| Regulation | 法规名称 | EU AI Act, GDPR |

**标注规则**：
- 只标注原文明确提及的实体
- 同一实体的不同表述归一化（如 "GPT-4" 和 "gpt4" 视为同一实体）
- 记录每个实体的 evidence_sentence 和 source_doc_id

#### Level 2：风险链标注（Stage 4 Layer 2 输出）

| 槽位 | 标注内容 | 示例 |
|------|---------|------|
| RiskSource | 导致风险的原因 | model hallucination in generated medical advice |
| Risk | 风险状态 | risk of AI-generated misinformation in medical guidance |
| Consequence | 直接后果 | users received inaccurate medical information |
| Impact | 实际影响 | potential harm to patient decision-making |
| AffectedActor | 受影响主体 | patients, healthcare providers |
| RiskControl | 控制措施 | system recall, content filtering |

**标注规则**：
- 允许合理抽象（不是逐字匹配原文）
- 每个槽位必须有至少一条 evidence_sentence 支撑
- 标注 extraction_mode：explicit（原文直接出现）或 abstracted（基于原文归纳）

#### Level 3：推理字段标注（Stage 4 Layer 3 输出）

| 字段 | 标注内容 | 示例 |
|------|---------|------|
| Purpose | AI 系统使用目的 | assist medical diagnosis |
| AILifecyclePhase | 风险发生阶段 | deployment |
| Domain | 受影响领域 | healthcare |

**标注规则**：
- 推理字段必须有 evidence + reasoning 支撑
- 标注 extraction_mode 为 inferred
- 数量较少，仅对 Gold Standard Set 标注

### 2.5 标注质量保障

| 机制 | 说明 |
|------|------|
| 标注员培训 | 标注前统一培训，提供标注指南和示例 |
| IAA 计算 | 随机抽取 20% 样本进行双盲标注，计算 Cohen's Kappa |
| Kappa 阈值 | Level 1 ≥ 0.85, Level 2 ≥ 0.75, Level 3 ≥ 0.70 |
| 争议解决 | 不一致样本由第三位标注员仲裁 |

### 2.6 Gold Standard 数据格式

```json
{
  "event_id": "pred_100",
  "gold_entities": [
    {
      "name": "K5 autonomous robot",
      "entity_type": "AISystem",
      "evidence_sentence": "the robot — a Knightscope K5 — ran itself right into a water fountain",
      "source_doc_id": "1035",
      "extraction_mode": "explicit"
    }
  ],
  "gold_risk_chain": {
    "risk_source": {
      "value": "autonomous navigation failure",
      "evidence": "Security robot falls into pond after failing to spot stairs or water",
      "mode": "abstracted"
    },
    "risk": {
      "value": "risk of autonomous system malfunction in public spaces",
      "evidence": "Security robot falls into pond after failing to spot stairs or water",
      "mode": "abstracted"
    },
    "consequence": {
      "value": "robot becomes non-functional",
      "evidence": "Security robot 'in critical condition' after nearly drowning on the job",
      "mode": "abstracted"
    },
    "impact": {
      "value": "disruption of security surveillance service",
      "evidence": "DC security robot quits job by drowning itself in a fountain",
      "mode": "abstracted"
    }
  },
  "gold_inferences": {
    "purpose": {
      "value": "autonomous security surveillance",
      "evidence": "The Knightscope K5 security robot's job is to be on the look out for crime.",
      "reasoning": "The description explicitly states the robot's purpose is security surveillance.",
      "mode": "inferred"
    },
    "lifecycle_phase": {
      "value": "deployment",
      "evidence": "a security robot face-planted into an indoor fountain inside a Washington, DC office building",
      "reasoning": "The robot was operating in a real-world office building, indicating deployment phase.",
      "mode": "inferred"
    },
    "domain": {
      "value": "public safety",
      "evidence": "The Knightscope K5 security robot's job is to be on the look out for crime.",
      "reasoning": "Security surveillance indicates the public safety domain.",
      "mode": "inferred"
    }
  }
}
```

---

## 3. 分层评测

### 3.1 显式实体评测（Stage 2）

#### 3.1.1 评测目标

评估 Pipeline 在单篇文档级别抽取显式实体的准确性。

#### 3.1.2 评测指标

| 指标 | 计算方式 |
|------|---------|
| **Precision** | TP / (TP + FP) |
| **Recall** | TP / (TP + FN) |
| **F1** | 2 × P × R / (P + R) |

#### 3.1.3 匹配规则

实体匹配采用三级判定：

| 判定 | 标准 | 示例 |
|------|------|------|
| **Exact Match** | 归一化后完全一致 | "ChatGPT" = "chatgpt" |
| **Partial Match** | 核心名称一致，修饰词不同 | "GPT-4" ≈ "GPT4" |
| **No Match** | 语义不同 | "ChatGPT" ≠ "Claude" |

**归一化规则**（复用项目已有 `src/core/ontology.py` 中的 `normalize_class` 和 `src/utils/text.py` 中的 `normalize_entity_name`）：

1. 统一转小写
2. 去除标点和空格
3. 应用 `CLASS_ALIASES` 同义词映射
4. 应用 `RELATION_ALIASES` 关系别名映射

#### 3.1.4 匹配计分

| 匹配级别 | TP 计分 |
|----------|---------|
| Exact Match | 1.0 |
| Partial Match | 视情况给分 |
| No Match | 0.0 |

#### 3.1.5 按实体类型分项评测

| 实体类型 | 预期样本量 | 难度 |
|----------|-----------|------|
| AISystem | 高 | 中 |
| AIModel | 中 | 中 |
| AITechnique | 低 | 低 |
| AICapability | 低 | 中 |
| Stakeholder | 高 | 低 |
| Regulation | 低 | 高 |

#### 3.1.6 输出格式

```
Table 1: Entity Extraction Results

| Entity Type  | Precision | Recall | F1    |
|-------------|-----------|--------|-------|
| AISystem    | xx.x%     | xx.x%  | xx.x% |
| AIModel     | xx.x%     | xx.x%  | xx.x% |
| AITechnique | xx.x%     | xx.x%  | xx.x% |
| AICapability| xx.x%     | xx.x%  | xx.x% |
| Stakeholder | xx.x%     | xx.x%  | xx.x% |
| Regulation  | xx.x%     | xx.x%  | xx.x% |
| **Overall** | **xx.x%** | **xx.x%** | **xx.x%** |
```

---

### 3.2 风险链评测（Stage 4 Layer 2）

#### 3.2.1 评测目标

评估事件级风险传播链（RiskSource → Risk → Consequence → Impact → AffectedActor）的抽取质量。**这是整个评测的核心。**

#### 3.2.2 评测方法

采用**语义匹配准确率**（Semantic Match Accuracy），由人工判定每个槽位的抽取结果与 Gold Standard 的语义一致性。

#### 3.2.3 判定标准

| 判定 | 标准 | 示例 |
|------|------|------|
| **Correct** | 语义一致，核心含义相同 | "model hallucination" ≈ "AI generating false information" |
| **Partial** | 部分正确，核心方向对但细节缺失或偏移 | "navigation failure" ≈ "robot malfunction"（偏笼统） |
| **Incorrect** | 语义不一致或完全错误 | "model hallucination" ≠ "data breach" |

#### 3.2.4 计算公式

```
Accuracy = (N_correct + Σ Partial Match Score × N_partial) / N_total
```

#### 3.2.5 按槽位分项评测

| 槽位 | 评测维度 | 预期难度 |
|------|---------|---------|
| RiskSource | 原因是否准确识别 | 高（需归纳） |
| Risk | 风险描述是否合理 | 中 |
| Consequence | 后果是否正确 | 中 |
| Impact | 影响是否恰当 | 高（需抽象） |
| AffectedActor | 受影响主体是否正确 | 低 |
| RiskControl | 控制措施是否完整 | 中（可选） |

#### 3.2.6 输出格式

```
Table 2: Risk Chain Accuracy

| Slot         | Correct | Partial | Incorrect | Accuracy |
|-------------|---------|---------|-----------|----------|
| RiskSource  | xx      | xx      | xx        | xx.x%    |
| Risk        | xx      | xx      | xx        | xx.x%    |
| Consequence | xx      | xx      | xx        | xx.x%    |
| Impact      | xx      | xx      | xx        | xx.x%    |
| AffectedActor| xx     | xx      | xx        | xx.x%    |
| **Overall** | **xx**  | **xx**  | **xx**    | **xx.x%**|
```

---

### 3.3 推理字段评测（Stage 4 Layer 3）

#### 3.3.1 评测目标

评估受控推理字段（Purpose, AILifecyclePhase, Domain）的准确性。

#### 3.3.2 评测指标

| 指标 | 定义 |
|------|------|
| **Accuracy** | 正确推理数 / 总推理数 |
| **Evidence Support Rate** | 有证据支撑的推理字段比例 |

#### 3.3.3 Evidence Support Rate 计算

```
Evidence Support Rate = N_inferred_with_valid_evidence / N_total_inferred
```

判定标准：
- 推理字段必须有 `evidence`（非空）且 `reasoning`（非空）
- evidence 句必须可在原文中找到对应
- reasoning 必须逻辑合理

#### 3.3.4 输出格式

```
Table 2b: Inference Field Accuracy

| Field            | Accuracy | Evidence Support Rate |
|-----------------|----------|----------------------|
| Purpose         | xx.x%    | xx.x%                |
| AILifecyclePhase| xx.x%    | xx.x%                |
| Domain          | xx.x%    | xx.x%                |
| **Overall**     | **xx.x%**| **xx.x%**            |
```

---

## 4. 事件级评测

### 4.1 Event-level Accuracy

#### 4.1.1 评测目标

评估一个事件中核心信息是否被正确抽取和表示。

#### 4.1.2 判定维度

| 维度 | 判定标准 |
|------|---------|
| AI System 正确性 | 事件涉及的核心 AI 系统是否被正确识别 |
| Risk 合理性 | 风险描述是否合理反映事件核心风险 |
| Consequence 合理性 | 后果是否正确反映事件实际后果 |
| 风险链完整性 | RiskSource → Risk → Consequence → Impact 链条是否完整 |

#### 4.1.3 事件级判定规则

| 判定 | 标准 |
|------|------|
| **Correct** | 3 个以上维度正确 |
| **Partial** | 2 个维度正确 |
| **Incorrect** | 0-1 个维度正确 |

```
Event Accuracy = (N_correct + 0.5 × N_partial) / N_total
```

### 4.2 Aggregation Gain（创新指标）

#### 4.2.1 评测目标

量化"多文档聚合"相比"单文档抽取"的提升幅度，验证事件级建模的有效性。

#### 4.2.2 实验设计

对同一事件，分别运行两种模式：

| 模式 | 说明 |
|------|------|
| **Single-doc** | 仅使用事件中第一篇报道进行抽取 |
| **Multi-doc** | 使用事件中所有报道进行聚合抽取 |

#### 4.2.3 对比维度

| 维度 | Single-doc | Multi-doc | Gain |
|------|-----------|-----------|------|
| 实体 Recall | R_single | R_multi | R_multi − R_single |
| Risk Accuracy | A_single | A_multi | A_multi − A_single |
| Event Accuracy | EA_single | EA_multi | EA_multi − EA_single |

#### 4.2.4 计算公式

```
Aggregation Gain (%) = (Multi-doc Score − Single-doc Score) / Single-doc Score × 100
```

#### 4.2.5 按事件规模分组分析

| 事件规模 | 报道数 | 预期 Gain |
|----------|--------|----------|
| Small | 1-5 | 低（信息增量小） |
| Medium | 6-30 | 中（跨文档互补明显） |
| Large | 30+ | 高（信息整合价值大） |

#### 4.2.6 输出格式

```
Table 3: Event-level Evaluation

| Method      | Entity Recall | Risk Accuracy | Event Accuracy |
|------------|---------------|---------------|----------------|
| Single-doc | xx.x%         | xx.x%         | xx.x%          |
| Multi-doc  | xx.x%         | xx.x%         | xx.x%          |
| **Gain**   | **+xx.x%**    | **+xx.x%**    | **+xx.x%**     |

Sub-group by event size:

| Event Size | Entity Recall Gain | Risk Accuracy Gain | Event Accuracy Gain |
|-----------|-------------------|-------------------|-------------------|
| Small     | +xx.x%            | +xx.x%            | +xx.x%            |
| Medium    | +xx.x%            | +xx.x%            | +xx.x%            |
| Large     | +xx.x%            | +xx.x%            | +xx.x%            |
```

---

## 5. 图谱级评测

### 5.1 Evidence Coverage

#### 5.1.1 定义

有证据支持的节点和关系的比例，衡量知识图谱的可追溯性。

#### 5.1.2 计算公式

```
Node Evidence Coverage = #nodes_with_evidence / #total_nodes (excluding AIRiskIncident)
Edge Evidence Coverage = #edges_with_evidence / #total_edges
Overall Evidence Coverage = (#nodes_with_evidence + #edges_with_evidence) / (#total_nodes + #total_edges)
```

#### 5.1.3 判定标准

| 条件 | 判定 |
|------|------|
| 节点有 ≥1 条 evidence_sentence 且 source_doc_id 有效 | 有证据 |
| 边有 ≥1 条 evidence_sentence 且 source_doc_id 有效 | 有证据 |

#### 5.1.4 按抽取模式分组

| 抽取模式 | 要求 | 预期覆盖率 |
|----------|------|-----------|
| explicit | 必须有证据 | ≥ 95% |
| abstracted | 必须有证据 | ≥ 90% |
| inferred | 必须有 evidence + reasoning | ≥ 85% |
| completed | 不要求（系统生成） | N/A |

### 5.2 Consistency Rate

#### 5.2.1 定义

无本体冲突的关系比例，衡量知识图谱的结构一致性。

#### 5.2.2 检查项

| 检查类型 | 说明 | 示例 |
|----------|------|------|
| **类型冲突** | 关系两端不符合本体域/范围约束 | Regulation → develops → AISystem ❌ |
| **逻辑冲突** | 语义上矛盾的关系 | Risk A → leadsTo → Consequence B 且 KnowledgeStatement A → conflictsWith → KnowledgeStatement B |
| **孤立节点** | 非事件节点无任何关系连接 | Stakeholder 节点无 roleHeldBy 等关系 |

#### 5.2.3 计算公式

```
Consistency Rate = 1 − (#type_violations + #logic_conflicts) / #total_edges
```

#### 5.2.4 实现基础

复用项目已有的验证逻辑：
- `src/agent/nodes/validation.py`：类型约束检查、证据约束检查、推理约束检查、风险链完整性检查
- `src/core/ontology.py`：`validate_relation()` 方法检查域/范围约束

### 5.3 Support Strength

#### 5.3.1 定义

平均 support_count，衡量知识是否被多篇报道支持。

#### 5.3.2 计算公式

```
Average Support Strength = Σ(support_count) / #total_nodes_and_edges
```

#### 5.3.3 按事件规模分组

| 事件规模 | 预期 Support Strength |
|----------|----------------------|
| Small (1-5) | 1.0-2.0 |
| Medium (6-30) | 3.0-8.0 |
| Large (30+) | 5.0-15.0 |

### 5.4 输出格式

```
Table 4: Graph Reliability

| Metric              | Value  |
|---------------------|--------|
| Node Evidence Coverage | xx.x% |
| Edge Evidence Coverage | xx.x% |
| Overall Evidence Coverage | xx.x% |
| Consistency Rate    | xx.x%  |
| Avg Support Strength | x.xx  |
```

---

## 6. 消融实验

### 6.1 消融实验 1：去掉事件聚合

#### 6.1.1 目的

验证事件级聚合（Stage 3）对最终结果的影响。

#### 6.1.2 实验设计

| 配置 | 说明 |
|------|------|
| **Full Pipeline** | 完整 5 阶段 Pipeline |
| **w/o Aggregation** | 跳过 Stage 3，直接将所有文档的显式实体拼接输入 Stage 4 |

#### 6.1.3 对比指标

| 指标 | 说明 |
|------|------|
| Risk Accuracy | 风险链抽取准确率 |
| Event Accuracy | 事件级准确率 |
| Entity Recall | 实体召回率 |
| Evidence Coverage | 证据覆盖率 |

#### 6.1.4 预期结果

去掉聚合后，中等和大事件的性能应显著下降，因为：
- 缺乏去重导致冗余实体干扰
- 缺乏代表性报道选择导致关键信息丢失
- 缺乏 support_count 导致置信度不准确

### 6.2 消融实验 2：去掉证据约束

#### 6.2.1 目的

验证证据约束机制对知识图谱可靠性的影响。

#### 6.2.2 实验设计

| 配置 | 说明 |
|------|------|
| **Full Pipeline** | 完整 Pipeline，含证据约束和验证 |
| **w/o Evidence Constraint** | 跳过 validation 节点，不强制 evidence/reasoning 约束 |

#### 6.2.3 对比指标

| 指标 | 说明 |
|------|------|
| Hallucination Rate | 无证据支撑的节点/关系比例 |
| Consistency Rate | 一致性比率 |
| Evidence Coverage | 证据覆盖率 |

#### 6.2.4 Hallucination Rate 定义

```
Hallucination Rate = #nodes_or_edges_without_valid_evidence / #total_nodes_and_edges
```

判定标准：
- explicit/abstracted 节点无 evidence_sentence → 幻觉
- inferred 节点无 evidence + reasoning → 幻觉
- 边无 evidence → 幻觉

#### 6.2.5 预期结果

去掉证据约束后：
- Hallucination Rate 显著上升
- Consistency Rate 下降
- 抽取量可能增加，但质量下降

### 6.3 消融实验输出格式

```
Table 5: Ablation Study

| Configuration           | Risk Acc | Event Acc | Evidence Cov | Hallucination Rate |
|------------------------|----------|-----------|-------------|-------------------|
| Full Pipeline          | xx.x%    | xx.x%     | xx.x%       | xx.x%             |
| w/o Aggregation        | xx.x%    | xx.x%     | xx.x%       | xx.x%             |
| w/o Evidence Constraint| xx.x%    | xx.x%     | xx.x%       | xx.x%             |
```

---

## 7. 效率评测

### 7.1 评测指标

| 指标 | 说明 |
|------|------|
| **Processing Time / Event** | 单个事件平均处理时间（秒） |
| **LLM Calls / Event** | 单个事件平均 LLM 调用次数 |
| **Cost / 1000 Events** | 处理 1000 个事件的 API 成本（美元） |
| **Token Usage / Event** | 单个事件平均 Token 消耗 |

### 7.2 按事件规模分组

| 事件规模 | 预期时间 | 预期 LLM 调用 | 预期 Token |
|----------|---------|--------------|-----------|
| Small (1-5) | 10-30s | 3-5 次 | 5K-15K |
| Medium (6-30) | 30-90s | 5-8 次 | 15K-40K |
| Large (30+) | 90-300s | 8-15 次 | 40K-100K |

### 7.3 对比实验

| 模式 | 说明 |
|------|------|
| Doc-level Pipeline | 对每篇报道独立运行完整 Pipeline |
| Event-level Pipeline | 本方法，事件级聚合后统一处理 |

### 7.4 输出格式

```
Table 6: Efficiency Evaluation

| Method       | Avg Time/Event | Avg LLM Calls | Avg Tokens | Cost/1000 Events |
|-------------|----------------|---------------|-----------|------------------|
| Doc-level   | xx.xs          | xx.x          | xx.xK     | $xx.xx           |
| Event-level | xx.xs          | xx.x          | xx.xK     | $xx.xx           |
```

---

## 8. 评测实施计划

### 8.1 阶段划分

| 阶段 | 任务 | 产出 |
|------|------|------|
| **Phase 1** | Gold Standard 标注 | 100 个事件的标注数据 |
| **Phase 2** | 运行 Pipeline 生成预测结果 | 500 个事件的 event_subgraph.json |
| **Phase 3** | 自动化评测脚本开发 | 评测代码 + Table 1, 4 |
| **Phase 4** | 人工评测 | Table 2, 2b, 3 |
| **Phase 5** | 消融实验 | Table 5 |
| **Phase 6** | 效率评测 | Table 6 |
| **Phase 7** | 结果汇总与分析 | 完整评测报告 |

### 8.2 评测脚本规划

| 脚本 | 功能 | 对应指标 |
|------|------|---------|
| `eval_entity.py` | 显式实体 P/R/F1 计算 | Table 1 |
| `eval_risk_chain.py` | 风险链语义匹配评测 | Table 2 |
| `eval_inference.py` | 推理字段准确率 + Evidence Support Rate | Table 2b |
| `eval_event_level.py` | 事件级准确率 + Aggregation Gain | Table 3 |
| `eval_graph_reliability.py` | Evidence Coverage / Consistency Rate / Support Strength | Table 4 |
| `eval_ablation.py` | 消融实验对比 | Table 5 |
| `eval_efficiency.py` | 效率指标统计 | Table 6 |

### 8.3 评测脚本存放位置

```
eval/
├── eval_entity.py
├── eval_risk_chain.py
├── eval_inference.py
├── eval_event_level.py
├── eval_graph_reliability.py
├── eval_ablation.py
├── eval_efficiency.py
├── gold_standard/           # Gold Standard 标注数据
│   ├── gold_100_events.json
│   └── labeling_guide.md
└── results/                 # 评测结果输出
    ├── table1_entity.json
    ├── table2_risk_chain.json
    ├── table2b_inference.json
    ├── table3_event_level.json
    ├── table4_reliability.json
    ├── table5_ablation.json
    └── table6_efficiency.json
```

---

## 9. Baseline 对比

### 9.1 对比方法

| 方法 | 说明 |
|------|------|
| **Direct Extraction** | 直接用 LLM 从全文抽取，无分阶段、无本体约束 |
| **Doc-level Pipeline** | 分阶段但无事件聚合，每篇文档独立抽取 |
| **Event-level w/o Evidence** | 事件级聚合但无证据约束 |
| **Full Pipeline (Ours)** | 完整方法：分阶段 + 事件聚合 + 证据约束 |

### 9.2 对比维度

| 维度 | Direct | Doc-level | w/o Evidence | Ours |
|------|--------|-----------|-------------|------|
| Entity F1 | - | - | - | - |
| Risk Accuracy | - | - | - | - |
| Event Accuracy | - | - | - | - |
| Evidence Coverage | - | - | - | - |
| Consistency Rate | - | - | - | - |
| Hallucination Rate | - | - | - | - |

---

## 10. 论文评测总结段（建议）

> We evaluate the proposed framework across three dimensions: extraction accuracy, event-level aggregation quality, and graph reliability. For extraction accuracy, we measure precision, recall, and F1 for explicit entity extraction (Stage 2) and semantic match accuracy for risk chain slot filling (Stage 4). For event-level aggregation quality, we introduce the Aggregation Gain metric to quantify the improvement of multi-document aggregation over single-document extraction. For graph reliability, we assess evidence coverage, ontology consistency rate, and support strength. Ablation studies demonstrate that event-centric aggregation improves risk understanding, while evidence-constrained extraction significantly enhances the reliability and traceability of the constructed knowledge graph.

---

## 附录 A：项目已有评测基础设施与差距分析

### A.1 已有组件

| 组件 | 文件 | 评测用途 |
|------|------|---------|
| 评测数据集 | `data/eval_dataset_500.json` | 500 个事件的评测输入 |
| 本体验证器 | `src/core/ontology.py` | Consistency Rate 计算基础 |
| 验证节点 | `src/agent/nodes/validation.py` | 类型/证据/推理/链完整性检查 |
| 本体约束 | `config/ontology.yml` | 关系域/范围约束定义 |
| 数据模型 | `src/core/models.py` | 41 类 + 37 关系的完整定义 |
| 测试套件 | `tests/` (302 cases) | Pipeline 各阶段功能测试 |
| 输出格式 | `output/<event_id>/event_subgraph.json` | 包含 nodes, edges, evidence, support_count |

### A.2 尚缺组件

| 缺失组件 | 对应评测 | 优先级 | 实现难度 |
|----------|---------|--------|---------|
| Gold Standard 标注数据 | 所有评测的基础 | **高** | 中（需人工） |
| 显式实体评测脚本 | Table 1 | **高** | 低 |
| 风险链语义匹配评测 | Table 2 | **高** | 中（需人工判定） |
| Aggregation Gain 对比实验 | Table 3 | **高** | 中 |
| 图谱可靠性评测脚本 | Table 4 | 中 | 低 |
| 消融实验框架 | Table 5 | 中 | 中 |
| 效率评测脚本 | Table 6 | 低 | 低 |

---

## 附录 B：评测数据流

```
data/eval_dataset_500.json
        │
        ▼
   ┌─────────────┐     ┌──────────────────┐
   │  Pipeline    │     │  Gold Standard    │
   │  (500 events)│     │  (100 events)     │
   └──────┬──────┘     └────────┬─────────┘
          │                      │
          ▼                      ▼
   event_subgraph.json     gold_100_events.json
          │                      │
          └──────────┬───────────┘
                     │
          ┌──────────┼──────────┐
          ▼          ▼          ▼
   eval_entity.py  eval_risk_chain.py  eval_graph_reliability.py
          │          │                  │
          ▼          ▼                  ▼
     Table 1      Table 2,2b         Table 4
                                       │
   eval_event_level.py                 │
          │                            │
          ▼                            │
     Table 3 ◄─────────────────────────┘
          │
   eval_ablation.py
          │
          ▼
     Table 5
          │
   eval_efficiency.py
          │
          ▼
     Table 6
```
